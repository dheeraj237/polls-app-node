<%- include('header') -%>
<div class="container">
    <div class="col col-md-6 align-self-center">
        <div class="row">
            <div class="col-md-6">
                <% if (error) { %>
                <p class="text-danger"><%= error %></p>
                <% } %>
                <a class="btn btn-secondary" onclick="window.history.back();" role="button">Go Back</a>
            </div>
        </div>

    </div>
</div>